import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'reto-ionic-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
